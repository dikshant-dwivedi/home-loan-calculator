generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Minimal model for health check - will expand later
model HealthCheck {
  id        String   @id @default(uuid())
  checkedAt DateTime @default(now())
}
